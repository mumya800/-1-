#include <stdio.h>

int main() {
    int a, b, c;
    printf("Введите три числа: ");
    scanf("%d %d %d", &a, &b, &c);
    
    int product_sign = (a == 0 || b == 0 || c == 0) * 0 + (a > 0) * (b > 0) * (c > 0) - (a < 0) * (b < 0) * (c < 0);
    
    printf("%d\n", product_sign);
    return 0;
}
